apiVersion: v1
kind: Service
metadata:
  name: authservice-frontend
  namespace: staging
  labels:
    app: authservice-frontend
    environment: staging
spec:
  selector:
    app: authservice-frontend
  ports:
  - port: 80
    targetPort: 80
    name: http
