apiVersion: v1
kind: Service
metadata:
  name: authservice-backend
  namespace: staging
  labels:
    app: authservice-backend
    environment: staging
spec:
  selector:
    app: authservice-backend
  ports:
  - port: 3010
    targetPort: 3010
    name: http
