apiVersion: apps/v1
kind: Deployment
metadata:
  name: authservice-backend
  namespace: staging
  labels:
    app: authservice-backend
    environment: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: authservice-backend
  template:
    metadata:
      labels:
        app: authservice-backend
        environment: staging
    spec:
      containers:
      - name: backend
        image: ghcr.io/apsferreira/auth-service-backend:staging
        imagePullPolicy: Always
        ports:
        - containerPort: 3010
          name: http
        env:
        - name: ENV
          value: "staging"
        - name: PORT
          value: "3010"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: authservice-staging-secrets
              key: database-url
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: authservice-staging-secrets
              key: jwt-secret
        - name: RESEND_API_KEY
          valueFrom:
            secretKeyRef:
              name: authservice-staging-secrets
              key: resend-api-key
        - name: RESEND_FROM_EMAIL
          valueFrom:
            secretKeyRef:
              name: authservice-staging-secrets
              key: resend-from-email
        - name: ALLOWED_ORIGINS
          value: "https://biblioteca.stg.institutoitinerante.com.br,https://focus-hub.stg.institutoitinerante.com.br,https://auth.stg.institutoitinerante.com.br"
        - name: JWT_ACCESS_EXPIRY
          value: "15m"
        - name: JWT_REFRESH_EXPIRY
          value: "168h"
        - name: OTP_EXPIRY_MINUTES
          value: "10"
        - name: SERVICE_TOKEN
          valueFrom:
            secretKeyRef:
              name: authservice-staging-secrets
              key: service-token
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "150m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3010
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3010
          initialDelaySeconds: 10
          periodSeconds: 5
